#########################################################
# Jethro SC490 Root & TWRP Installation Script
# Requirements: SC490, USB cable, computer with ADB/Fastboot
# WARNING: Unlocking bootloader WILL erase your data!
#########################################################

# -------- Step 0: Set up ADB & Fastboot --------
# Download Platform Tools: 
https://developer.android.com/studio/releases/platform-tools
#Download this very important zip for root
https://github.com/Garbagebuild/SC490-Jethro-root/releases
# Extract to e.g., C:\platform-tools
# Open terminal in that folder
adb version
fastboot version
# -------- Step 1: Enable Developer Options & USB Debugging --------
# On your phone:
# Settings → System → About Phone → Tap Build Number 7x
# Settings → System → Developer Options → Enable "OEM Unlocking" & "ADB Debugging"

# -------- Step 2: Boot into Fastboot Mode --------
# Power down fully (or remove battery if removable)
# Hold Volume Up + Power button until this appears
===========================
       Select Boot Mode
===========================
 
  [Recovery Mode
  [Fastboot Mode
  [Normal Boot
===========================
Use VOLUME_UP to select.
VOLUME_DOWN is OK.
===========================

# Press Volume Up once, then select Fastboot with Volume Down

# Connect phone to computer via USB

# -------- Step 3: Unlock Bootloader (Optional/Risky) --------
fastboot oem unlock
# WARNING: This will likely wipe your data!

# -------- Step 4: Flash Boot & Recovery Images --------
# Replace paths with the actual paths of your files
fastboot flash boot "E:\path\to\boot.img"
fastboot flash recovery "E:\path\to\recovery.img"
fastboot reboot
Hold volume up + power button as it is booting

# -------- Step 5: Use TWRP Recovery --------
# Boot into TWRP from menu using volume down when you are in the
===========================
       Select Boot Mode
===========================
 
  [Recovery Mode
  [Fastboot Mode
  [Normal Boot
===========================
Use VOLUME_UP to select.
VOLUME_DOWN is OK.
===========================
# Push Magisk database to phone
adb push "E:\path\to\magisk.db" /data/data/adb/

# Reboot phone from TWRP
adb reboot

# -------- Step 6: Test Root Access --------
adb shell
su
# If prompt changes from $ to #, root is successful!
#########################################################
